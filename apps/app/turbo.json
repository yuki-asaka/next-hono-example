{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["DATABASE_URL", "PORT"]
    },
    "lint": {
      "env": ["DATABASE_URL", "PORT"]
    }
  }
}